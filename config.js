var config = {};

config.port = 3000;

// Store all credentials and configs in this file
// Example using redis:
config.redis = {
    "PORT": 6379,
    "HOST": "cintricdash.redis.cache.windows.net",
    "KEY": "Enter key here"
};

module.exports = config;